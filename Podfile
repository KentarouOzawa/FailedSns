# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'failedSns' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for failedSns

  target 'failedSnsTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'failedSnsUITests' do
    inherit! :search_paths
    # Pods for testing
  end
pod 'CDAlertView'
pod 'Firebase/Auth'
pod 'Firebase/Storage'
pod 'PINRemoteImage' 
pod 'PKHUD', '~> 5.0'
pod 'Firebase/Core'
pod 'Firebase/Firestore'

end
